version: '3.9'

services:
  db:
    image: postgres:16
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    environment:
      POSTGRES_DB: torquemate
      POSTGRES_PASSWORD: fordevonly
    volumes:
      - ./migrations/bootstrap.sql:/docker-entrypoint-initdb.d/bootstrap.sql
    ports:
      - 5432:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

  # torquemate:
  #   image: torquemate
  #   environment:
  #     ConnectionStrings__NpgsqlTorqueDatabase: Host=db; Port=5432; Database=torquemate; Username=postgres; Password=fordevonly; Include Error Detail=true;
  #   ports:
  #     - 5147:8080
